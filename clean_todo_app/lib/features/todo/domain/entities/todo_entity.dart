class TodoEntity {
    final String id;
    final String title;
    final bool done;

    const TodoEntity ({
        required this.id,
        required this.title,
        required this.done,
    });
}